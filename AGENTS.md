# AGENTS.md

Operational notes for future coding agents working in this repository.

## Project Overview

Prism is a Rust/Tauri v2 + React/TypeScript desktop scanner for competitive Roblox FastFlag abuse. Players run it before matches; staff review signed JSON reports.

The important security problem is detecting non-allowlisted Roblox FFlags in three places:

- On disk, especially `ClientAppSettings.json` and bootstrapper config folders.
- In known injector/tool artifacts, via process, file, prefetch, hashes, paths, and heap markers.
- In the live Roblox process, where injectors patch FastFlag runtime values directly in memory.

Current prepared release in this workspace is `v0.8.1` / package version `0.8.1`.
Prism is distributed as a portable Windows executable, not an installer. Keep
Tauri bundling disabled for release builds unless the user explicitly changes
the distribution model.

## Tech Stack

- Frontend: React 19, TypeScript, Vite, Vitest.
- Desktop shell: Tauri v2.
- Backend/scanners: Rust crate `prism`.
- Scanner library name: `prism_lib`.
- Workspace members:
  - `src-tauri/`
  - `src-tauri/tools/memory_scan_harness/`
  - `src-tauri/tools/memory_evidence_exporter/`

## Common Commands

From repo root:

```powershell
npm install
npm run test
npm run build
npm run tauri dev
npm run tauri -- build --no-bundle
```

From `src-tauri/`:

```powershell
cargo test -p prism --lib
cargo test -p prism --lib memory_scanner
cargo build --release -p memory-scan-harness
cargo build --release -p memory-evidence-exporter
.\target\release\memory-scan-harness.exe
```

The memory harness prints human-readable findings to stderr and JSON to stdout.

## Repository Map

- `src/`
  - React frontend.
  - `App.tsx` is the main UI.
  - `App.test.ts` contains frontend tests.
- `src-tauri/src/commands.rs`
  - Tauri commands: scan, save/import/validate reports.
- `src-tauri/src/scanners/mod.rs`
  - Scanner orchestration and filtering of stale file findings.
- `src-tauri/src/scanners/process_scanner.rs`
  - Running-process detection.
- `src-tauri/src/scanners/file_scanner.rs`
  - Filesystem artifact, directory, config, hash, and known payload detection.
- `src-tauri/src/scanners/client_settings_scanner.rs`
  - Roblox `ClientAppSettings.json` and bootstrapper config parsing.
- `src-tauri/src/scanners/prefetch_scanner.rs`
  - Windows Prefetch execution-history scanner.
- `src-tauri/src/scanners/memory_scanner.rs`
  - Main live Roblox memory scanner. Large single file; treat edits as high risk.
- `src-tauri/src/data/known_tools.rs`
  - Known tool names, dirs, markers, hashes, binary fingerprints.
- `src-tauri/src/data/suspicious_flags.rs`
  - Critical/high/medium/low non-allowlisted FastFlag rules.
- `src-tauri/src/data/flag_allowlist.rs`
  - Roblox official allowlist. Keep separate from suspicious rules.
- `src-tauri/src/models/`
  - `ScanFinding`, `ScanVerdict`, signed `ScanReport`.
- `src-tauri/src/reports/report_generator.rs`
  - Report saving and signing.
- `src-tauri/tools/memory_scan_harness/`
  - CLI harness for the memory scanner without Tauri UI.
- `src-tauri/tools/memory_evidence_exporter/`
  - Dumps ASCII/UTF-16 heap hits for offline review.

## Git/Workspace Caution

This workspace often contains local investigation artifacts: `harness_*.log`, `harness_*.json`, `evidence_*.json`, extracted strings, local injector samples, and release assets.

Do not delete or revert those unless the user explicitly asks. Also do not revert unrelated tracked changes. Before editing, check:

```powershell
git status --short
git diff -- <file>
```

Use `apply_patch` for manual edits.

## Scanner Verdict Model

Verdicts are:

- `Clean`: informational or no abuse evidence.
- `Suspicious`: plausible abuse signal needing review.
- `Flagged`: high-confidence abuse evidence.

The memory string-scan value-match path is deliberately capped at `Suspicious` unless it has stronger context. Live registry overrides can be `Flagged` when a curated cheat value is read from the actual runtime value storage.

Reports are HMAC-SHA256 signed with a per-build key generated by `build.rs`. This is client-side integrity only; do not oversell it as server-grade attestation.

## Roblox Trust/Path Rules

The scanner should accept legitimate Roblox install roots:

- `%LocalAppData%\Roblox\Versions\`
- `%LocalAppData%\Bloxstrap\Versions\`
- `%LocalAppData%\Fishstrap\Versions\`
- Roblox UWP package-family paths scoped to `ROBLOXCORPORATION.`

The memory scanner rejects Roblox-named decoy processes outside trusted roots.

Legitimate WebView2 modules under `Program Files*\Microsoft\EdgeWebView\Application\` are trusted module paths. `EmbeddedBrowserWebView.dll` is legitimately loaded by Roblox.

## FFlag Allowlist Rules

Roblox's official allowlist is handled in `flag_allowlist.rs`. Do not duplicate official allowlisted flags into suspicious tiers unless policy explicitly changes. If a flag is officially allowlisted, file/client-settings scanners should not warn solely because it appears.

Suspicious rule tiers live in `suspicious_flags.rs`. Runtime override rules in `memory_scanner.rs` should stay curated and value-specific.

## Memory Scanner Architecture

Windows only. macOS memory scanning is intentionally not implemented.

Key Windows APIs:

- `OpenProcess`
- `VirtualQueryEx`
- `ReadProcessMemory`
- module enumeration through PSAPI

Core scanner behavior:

- Enumerates Roblox process memory.
- Skips unsuitable regions and tracks coverage gaps.
- Scans heap for ASCII/UTF-16 FastFlag names.
- Tracks injector/tool markers near parsed flag values.
- Enumerates modules and flags untrusted loaded DLLs.
- Attempts live FastFlag registry inspection to read the values the Roblox engine actually uses.

Important memory scanner helpers and concepts:

- `RUNTIME_OVERRIDE_RULES`: curated cheat name/value pairs.
- `RUNTIME_FLAG_BASELINES`: known vanilla defaults used to detect arbitrary deviations.
- `RuntimeRegistryCandidate`: candidate live FastFlag registry singleton/table.
- `lookup_runtime_flag_entry`: directed lookup using hash + bucket walk.
- `scan_runtime_registry_interesting_entries`: fallback bucket enumeration for known/probe/baseline flags.
- `collect_fflag_shaped_entries`: bucket enumeration that yields *any* FFlag-shaped node, not just the curated set. Feeds the generic singleton-injection detection.
- `inspect_generic_singleton_overrides` + `aggregate_generic_singleton_finding`: build-agnostic detection that flags any singleton-walking FFlag injector by comparing live `entry+0xC0` values against the Roblox executable's shipped defaults at the same RVA. Filters out the official Roblox allowlist and `MEMORY_BASELINE_FLAGS`. Verdict: any non-allowlisted CRITICAL deviation OR ≥3 non-allowlisted deviations → Flagged; 1–2 non-critical → Suspicious.
- `RUNTIME_VALUE_PTR_OFFSET`: entry field offset to the actual value pointer, currently `0xC0`.
- `NodeLayout`: candidate hash-node layouts for Roblox/STL changes.
- Anchor/pointer discovery paths are diagnostic/fallback scaffolding and can be noisy.

## Generic Singleton-Injection Detection

The detection invariant exploited by `inspect_generic_singleton_overrides`:
**every singleton FFlag injector writes to the 4-byte storage slot reached via
`entry+0xC0`** after walking the FastFlag hash table. The vanilla state of
that slot is whatever the Roblox PE shipped in its `.data` section at the
same RVA. Whoever did the write — TSBAH, MxStrap, LornoFix, a future tool —
leaves the same fingerprint: the live i32 diverges from the on-disk default.

The new path runs alongside the curated `RUNTIME_OVERRIDE_RULES` /
`RUNTIME_FLAG_BASELINES` paths inside the singleton-resolution loop. For each
discovered singleton candidate, it walks the bucket chain, collects every
node whose name matches `is_fflag_shaped_name` (any `FLAG_PREFIXES` prefix +
a valid identifier body), reads the live value, and compares against
`PeImageDefaults::read_i32_at_rva`. Deviations on flags in the official
allowlist (`is_allowed_flag`) or on `MEMORY_BASELINE_FLAGS` (names Roblox
itself overwrites at runtime via telemetry / A-B rollout) are suppressed.
Remaining deviations roll into one summary `ScanFinding` via the pure-fn
`aggregate_generic_singleton_finding` aggregator (unit-tested without a live
handle).

Why this matters: the curated lists in `RUNTIME_OVERRIDE_RULES` and
`RUNTIME_FLAG_BASELINES` cover ~30 flag names total. Singleton injectors
routinely write to flags outside both lists (e.g. the modern RakNet /
RenderConfig set: `DFIntRakNetPingFrequencyMillisecond`,
`DFIntRakNetLoopMs`, `DFIntMaxDataPacketPerSend`, …). The generic path
catches those without curating each one individually. Curation discipline
still matters for the curated paths — they produce specific, named findings
with per-flag severity context — but the generic path is the safety net.

### Key correctness rules

- **Roblox stores BARE names in the FVar registry**, not the developer-facing
  full identifier. `S2PhysicsSenderRate`, not `DFIntS2PhysicsSenderRate`. The
  prism helper `classify_runtime_key` bridges this by trying each
  `FLAG_PREFIXES` entry concatenated with the bare key against every curated
  list. `is_fflag_shaped_name` accepts identifier-shaped names *without*
  requiring a prefix — filtering by prefix would reject almost every real
  flag.
- **Non-critical deviations cap at Suspicious**, mirroring the trust-model
  rule for the heap string-scan path. Roblox itself overwrites a long tail
  of telemetry / A-B rollout flags on every live client, and the disk
  image's `.data` defaults are the *initial* zero-state rather than the
  post-bootstrap state. Counting "many non-critical deviations" as Flagged
  would mis-flag every vanilla client. Critical-list deviations
  (`suspicious_flags::CRITICAL_FLAGS` matched via the prefixed-name lookup)
  remain the only path to a Flagged verdict from this detection.
- **Magnitude filter** (`deviation_within_int_magnitude`,
  `GENERIC_SINGLETON_VALUE_MAGNITUDE_CAP = 50_000_000`) suppresses
  type-confused reads. FVar value cells are uniformly 4 bytes but store
  different concrete types — bool, i32, f32, or the first 4 bytes of an
  8-byte std::string head. Reading 4 bytes as i32 from a string-pointer
  cell produces values in heap-pointer ranges (often >100M, often
  >1G); from a float cell it produces float bit-patterns that look like
  random large integers. Both halves of the comparison must fit in the
  magnitude window before the deviation is counted.
- **The curated `MEMORY_BASELINE_FLAGS` list is known incomplete.** A live
  Roblox client routinely shows ~500–700 non-critical deviations from
  `.data` defaults via the generic path — most are Roblox-internal flags
  that should eventually be added to `MEMORY_BASELINE_FLAGS`. Until that
  list expands, treat the per-flag deviation listing as triage material,
  not as a list of injections.
- **Generic-path verdict is capped at Suspicious; Flagged comes from the
  curated paths.** The generic path emits curated-style Critical findings
  for flags whose name matches `RUNTIME_OVERRIDE_RULES` exact cheat values
  — this is the bare-key bridge: the legacy
  `inspect_runtime_fflag_registry` curated path FNV-1a-hashes the
  developer-facing prefixed name (`DFIntS2PhysicsSenderRate`), but Roblox
  keys the FVar table by the bare form (`S2PhysicsSenderRate`), so the
  legacy hash lookup misses every prefixed flag. The bare-key bucket walk
  in `collect_fflag_shaped_entries` resolves the entry, and
  `inspect_generic_singleton_overrides` then emits the curated Critical
  finding directly for the matched flag.
- **Bucket-walk coverage requires the right cap.** Live Roblox tables hold
  20k–30k FFlags; `GENERIC_FFLAG_ENTRY_CAP` is set to 65,536 so the walk
  doesn't break out of its outer layout loop before reaching the late
  buckets. At a lower cap (16,384 was the original value) entries whose
  bucket happens to be enumerated late were silently skipped — verified
  by directed `lookup_runtime_flag_entry` finding `S2PhysicsSenderRate`
  while the enumeration walk missed it. Raising the cap fixed it.
- **Use `read_remote_node_string_relaxed` in enumeration, not the strict
  reader.** The strict `read_remote_node_string` rejects nodes with
  `cap > MAX_IDENT_BODY_LEN + 32`, but live Roblox `std::string`
  allocations frequently report larger capacities for heap-grown strings.
  The directed `remote_node_string_matches` lookup doesn't have that
  upper-bound check (it falls back to comparing the inline byte slot
  against the expected name), so it reaches entries the strict reader
  silently drops. The relaxed reader replicates that tolerance for the
  enumeration path: try inline first, then dereference the heap pointer
  without an artificial cap ceiling.

## Current In-Memory FFlag Detection Context

Tool-level detection of injectors already works well: process/file/hash/marker/binary-fingerprint coverage catches known tool families and MxStrap-like payloads. The hard part is detecting the cheat effect after the tool exits or hides.

Observed injector model:

1. Locate Roblox's FastFlag registry singleton.
2. Resolve `singleton + 0x8` to a hash-table header.
3. Hash the flag name and walk the bucket chain.
4. Find a node with the matching name string.
5. Read the node entry pointer.
6. Read `entry + 0xC0` to get the runtime value pointer.
7. Write 4 bytes to the runtime value storage with `WriteProcessMemory`.

Offsets confirmed from the working payload lineage:

- Table header from singleton: `+0x8`
- Table size: `0x38`
- Bucket stride: `0x10`
- Standard node:
  - string/pointer field: `+0x10`
  - length: `+0x20`
  - capacity: `+0x28`
  - entry pointer: `+0x30`
  - next pointer: `+0x08`
- Entry runtime value pointer: `entry + 0xC0`
- Hash constants observed in payload: FNV-1a 64 offset `0xcbf29ce484222325`, prime `0x100000001b3`

The directed hash lookup may still fail on newer Roblox clients if the table/hash/bucket semantics drift. Keep the bucket-enumeration fallback because it reads bucket chains directly and matches known names without relying on the hash computation.

Roblox currently obfuscates many flag names in the binary. Do not assume flag names appear as plain ASCII in `.rdata`; they often appear only after runtime decryption in heap regions separate from value storage.

## Current Injector Notes

Old Lorno-specific assumptions should not drive new work. The user no longer uses Lorno as the active reproducer.

Modern injector payloads may have a full UI wrapper. Focus on:

- Live Roblox memory effects.
- Runtime values.
- Generic `WriteProcessMemory` behavior.
- Stable registry/node layout signals.
- Tool artifacts only as supporting evidence.

Local samples may exist in the repo root or Downloads, but paths are not guaranteed stable.

### MxStrap / RenderConfig Address Cache

The current UI injector observed on 2026-05-16 writes a per-Roblox-PID address cache under:

```text
%APPDATA%\Microsoft\RenderConfig\flags_<pid>.json
```

Example current live path:

```text
C:\Users\Evelyn\AppData\Roaming\Microsoft\RenderConfig\flags_8068.json
```

This cache maps stripped FastVariable names to absolute 64-bit value addresses in the live Roblox process. Example keys:

- `SecondsBetweenDynamicVariableReloading`
- `RakNetPingFrequencyMillisecond`
- `RakNetLoopMs`
- `WaitOnRecvFromLoopEndedMS`
- `WaitOnUpdateNetworkLoopEndedMS`
- `RaknetBandwidthPingSendEveryXSeconds`
- `RaknetBandwidthInfluxHundredthsPercentageV2`
- `RakNetNakResendDelayMsMax`
- `RakNetNakResendDelayMs`

The memory scanner now has a RenderConfig fallback path:

1. Look for `flags_<pid>.json`.
2. Strip known prefixes from `RUNTIME_OVERRIDE_RULES` names (`DFInt`, `FInt`, `DFFlag`, etc.) to match the cache keys.
3. Read each cached live address with `ReadProcessMemory`.
4. Elevate only when the live value differs from the same RVA in the Roblox executable on disk. Exact curated values are not counted if the value is still the executable default.
5. If the current-PID cache is missing or yields no live diff, probe the newest stale `flags_*.json` files as a fallback, but only count addresses that validate against the current Roblox module and differ from the executable default.
6. Flag when at least three live changed values match; one or two matches are suspicious.

This is not the same as solving the hash-table singleton walk. It is a pragmatic current-build/current-injector detection path that confirms the live cheat effect from memory while using the injector's address cache as a map.

Important safety behavior: the current-PID cache is trusted as context, but stale caches are used only when their addresses still validate in the current Roblox process and the current live value differs from the executable default.

The client-settings scanner also reports `%APPDATA%\Microsoft\RenderConfig\render_cfg.dat` and recent `flags_*.json` files as suspicious tool artifacts. This catches lazy cache deletion / PID-mismatch cases as file evidence, but does not claim live memory proof unless the memory scanner can still validate current Roblox memory.

Current injected repro values confirmed live in Roblox via the RenderConfig cache:

- `DFIntSecondsBetweenDynamicVariableReloading=9999`
- `DFIntRakNetPingFrequencyMillisecond=1`
- `DFIntRakNetLoopMs=1`
- `DFIntWaitOnRecvFromLoopEndedMS=1`
- `DFIntWaitOnUpdateNetworkLoopEndedMS=10`
- `DFIntRaknetBandwidthPingSendEveryXSeconds=1`
- `DFIntRaknetBandwidthInfluxHundredthsPercentageV2=10000`
- `DFIntRakNetNakResendDelayMsMax=1`
- `DFIntRakNetNakResendDelayMs=1`
- `DFIntActionStationDebounceTime=0`
- `DFIntSafetyServiceScreenshotAMCDebouncePeriodMilliseconds=0`
- `DFIntSimTimestepMultiplierDebounceCount=0`
- `DFIntSimTouchDebounceEntryLimit=32`
- `FIntActivatedCountTimerMSMouse=0`
- `FIntActivatedCountTimerMSKeyboard=0`
- `FIntCLI20390_2=0`
- `DFIntClientPacketMaxDelayMs=0`
- `DFIntRenderingThrottleDelayInMS=1`
- `DFIntMaxProcessPacketsStepsAccumulated=0`
- `DFIntMaxDataPacketPerSend=100000`
- `DFIntLargePacketQueueSizeCutoffMB=1000`
- `DFIntCodecMaxOutgoingFrames=10000`
- `DFIntCodecMaxIncomingPackets=100`
- `FLogNetwork=7`
- `DFIntMaxProcessPacketsStepsPerCyclic=5000`
- `DFIntMaxProcessPacketsJobScaling=10000`

The user's full config also included several `false` boolean debounce flags and AICO/Lua autocomplete debounce values. In the current repro, the per-PID address cache only exposed the 26 values above, so Prism treats those 26 as live memory proof and does not claim the missing config-only flags were actually written.

Harness output after the patch should include:

```text
Flagged memory_scanner - Critical live Roblox runtime-variable overrides
```

## Live Repro Workflow

When validating in-memory detection:

1. Start Roblox through a legitimate launcher such as Bloxstrap/Fishstrap.
2. Stay pre-join or in a non-Hyperion place if memory reads are needed.
3. Run the current injector and apply target FFlags.
4. Confirm the cheat effect if the user is doing manual repro.
5. From `src-tauri/`, build and run:

```powershell
cargo build --release -p memory-scan-harness
.\target\release\memory-scan-harness.exe
```

Expected useful diagnostics:

- Number of singleton candidates.
- Heap table header candidates kept/matched.
- Registry entries read.
- Live registry override findings.
- Anchor coverage.
- Binary cheat-value diagnostic counts.
- Memory coverage failures/read-denied regions.

If no Roblox process is running, the harness correctly returns an `Inconclusive` skip.

## Hyperion Caveat

Once Roblox joins some Hyperion-protected experiences, `OpenProcess` / memory reads may fail even as Administrator. Do memory scanning before Hyperion arms when investigating live registry behavior.

## Static Analysis Tips

For Roblox or injector binaries:

- Python `pefile` is useful for PE structure.
- Capstone can be installed locally for disassembly if needed.
- Search for import calls to `ReadProcessMemory` and `WriteProcessMemory`.
- In injector payloads, look for strings such as:
  - `fflags.json`
  - `address.json`
  - `singleton`
  - `found singleton [pattern]`
  - `failed to find pattern`

In the known payload, the lookup/write path showed:

- `ReadProcessMemory` reads node/entry/value fields.
- `WriteProcessMemory` writes either 4-byte values or string payloads depending on flag type.
- Bool/int writes use the entry returned by the registry lookup path.

## Release/Version Notes

Keep Rust crate version, npm package version, and release tags aligned.

Known release state in this workspace:

- `package.json`: `0.8.1`
- `src-tauri/Cargo.toml`: `0.8.1`
- `src-tauri/tauri.conf.json`: `0.8.1`
- Last completed local build: `v0.8.1`

## Testing Expectations

For narrow scanner changes:

```powershell
cd src-tauri
cargo test -p prism --lib memory_scanner
```

For broader backend changes:

```powershell
cd src-tauri
cargo test -p prism --lib
```

For frontend changes:

```powershell
npm run test
npm run build
```

For memory scanner live validation:

```powershell
cd src-tauri
cargo build --release -p memory-scan-harness
.\target\release\memory-scan-harness.exe
```

## Coding Style

- Keep changes scoped. `memory_scanner.rs` is large and easy to destabilize.
- Prefer existing helper functions and data tables.
- Add tests for false-positive regressions, especially around allowlists and trusted paths.
- Do not make bare heap string presence a hard accusation.
- Keep low-confidence heap heuristics capped at `Suspicious`.
- Avoid adding broad trusted directories; trust specific publisher/runtime roots.
- Avoid destructive git commands.

## Recent Local Patch Context

The current memory scanner includes a bucket-enumeration fallback for live registry candidates and a WebView2 trust-path regression test. If validating the FFlag-memory work, first run the harness against an actively injected Roblox session and check whether "Registry entries read" rises above the old 1-entry failure pattern.
