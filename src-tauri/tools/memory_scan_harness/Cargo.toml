[package]
name = "memory-scan-harness"
version = "0.0.1"
edition = "2021"
publish = false

[dependencies]
prism = { path = "../..", default-features = false }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
